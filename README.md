# image-integration
This project demonstrates how to embed and display digital images within a webpage using HTML.

The primary goal is to master the implementation of the static image tag alongside its core attributes like src (source path), alt (alternative text), and dimension controls (width and height).
